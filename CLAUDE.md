# CLAUDE.md - Social Stream Ninja Standalone (ssapp)

Read `AGENTS.md` for full project guidance (source-of-truth rules, build/release rules, testing requirements, code style).

## Git Rules (CRITICAL)

- **Never create git branches, worktrees, or pull requests in this repo unless Steve explicitly asks for one.** Work directly on the currently checked-out branch (normally `main`) in the main checkout at `C:\Users\steve\Code\ssapp`.
- Agent-created branches and worktrees keep getting orphaned and their work lost. Do not add more.
- If a task genuinely seems to require a branch or PR, stop and ask Steve first.
