{
  "name": "socialstream-sso-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker for Social Stream OAuth broker endpoints.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
